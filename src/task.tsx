//API_KEY to env
//зробити іконки svg air polutants та weather conditions
//переробити іконки стану погоди
//add loading spinner
// onClick={() => {
// if (onClick) onClick(); // викликаємо onClick
// if (onHide) onHide(); // викликаємо onHide, якщо він є
// onClick = () => ErrorStore.clearErrors();
//звернути на це увагу можливо on hide  можна буде переписати
